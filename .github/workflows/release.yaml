name: Release
on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: publish jar
        run: |
          VERSION=$(echo ${GITHUB_REF} | sed -e "s/refs\/tags\/v//")
          gradle build publish -x test -Pversion=$VERSION
        env:
          USERNAME: rotilho
          TOKEN: ${{ secrets.TOKEN }}